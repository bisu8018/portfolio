/**
 * Footer 컴포넌트
 * 하단 푸터 영역을 담당합니다.
 * @component
 * @returns {JSX.Element} 푸터 영역
 */
export default function Footer() {
  return (
    <footer className="fixed inset-x-0 bottom-0 w-screen text-center p-2 text-xs  text-white">
      창 닫으면 아이콘 추가
    </footer>
  )
}
